name: Terraform Plan

on:
  pull_request:
    branches: [main]
    paths:
      - "terraform/**"
      - ".github/workflows/terraform-plan.yml"

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  precheck:
    name: Check secrets configured
    runs-on: ubuntu-latest
    outputs:
      gcp_configured: ${{ steps.check.outputs.gcp_configured }}
    steps:
      - id: check
        run: |
          if [ -n "${{ secrets.GCP_WIF_PROVIDER }}" ] && [ -n "${{ secrets.GCP_DEPLOY_SA }}" ] && [ -n "${{ secrets.GCP_PROJECT_ID }}" ]; then
            echo "gcp_configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "gcp_configured=false" >> "$GITHUB_OUTPUT"
            echo "::warning::GCP secrets not configured. Skipping plan job."
          fi

  plan:
    name: Plan ${{ matrix.env }}
    runs-on: ubuntu-latest
    needs: [precheck]
    if: needs.precheck.outputs.gcp_configured == 'true'
    strategy:
      fail-fast: false
      matrix:
        env: [dev, staging]
    steps:
      - uses: actions/checkout@v5

      - id: auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOY_SA }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.5

      - name: terraform init
        working-directory: terraform/environments/${{ matrix.env }}
        run: |
          terraform init \
            -backend-config="bucket=tfstate-${{ secrets.GCP_PROJECT_ID }}" \
            -backend-config="prefix=genai-rag/${{ matrix.env }}"

      - name: terraform plan
        working-directory: terraform/environments/${{ matrix.env }}
        run: |
          terraform plan \
            -var "project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -out=tfplan -no-color | tee plan.out

      - name: Comment plan on PR
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('terraform/environments/${{ matrix.env }}/plan.out', 'utf8');
            const body = `### terraform plan — ${{ matrix.env }}\n\n\`\`\`hcl\n${plan.slice(0, 60000)}\n\`\`\``;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
