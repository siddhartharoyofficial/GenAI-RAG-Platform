# Developer dependencies — lint, format, type-check, unit-test.
# Deliberately does NOT install the runtime deps; CI doesn't need them to lint.
# If you want the full stack for local dev, run:
#   uv pip install -r requirements/requirements.txt -r requirements/requirements-dev.txt

# Linting / formatting
ruff>=0.7,<1
black>=24.10,<25
mypy>=1.13,<2

# Testing
pytest>=8.3,<9
pytest-asyncio>=0.24,<1
pytest-cov>=6.0,<7
httpx>=0.27,<1

# Pre-commit
pre-commit>=4.0,<5
