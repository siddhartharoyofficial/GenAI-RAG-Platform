apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  namespace: genai-rag
  annotations:
    # Bound to the GSA via Workload Identity (see terraform/modules/iam).
    iam.gke.io/gcp-service-account: REPLACE_WITH_GSA_EMAIL
